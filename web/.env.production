# Production Environment Configuration for EchosOfMe
# This file contains production environment variables for deployment

# NextAuth Configuration
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=https://your-production-domain.com

# Database Configuration - Neon PostgreSQL
# Replace with your Neon connection string
DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Redis Configuration - Upstash
# Replace with your Upstash Redis URL
REDIS_URL=rediss://default:password@region.upstash.io:port

# RTX 5090 Local GPU Configuration
# This maintains the hybrid architecture with local GPU processing
RTX_GPU_ENDPOINT=https://your-cloudflare-tunnel.trycloudflare.com
RTX_GPU_API_KEY=your-rtx-api-key

# ML Inference Configuration
# For cloud-based inference when RTX is unavailable
ML_INFERENCE_URL=https://your-ml-service.com/api
ML_INFERENCE_API_KEY=your-ml-api-key

# Email Configuration (for production notifications)
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASS=your-email-password

# Application Settings
NODE_ENV=production
PORT=3000

# Security Configuration
CORS_ORIGIN=https://your-production-domain.com
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=15

# Monitoring and Analytics
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
ANALYTICS_API_KEY=your-analytics-key

# Feature Flags
ENABLE_RTX_FALLBACK=true
ENABLE_VOICE_TRAINING=true
ENABLE_ADMIN_FEATURES=true

# Cache Configuration
CACHE_TTL=3600
ENABLE_QUERY_CACHE=true

# File Storage (if needed)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_BUCKET_NAME=your-s3-bucket
# AWS_REGION=us-east-1